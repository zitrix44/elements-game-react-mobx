import elements_logicErrors from './elements.logicErrors';
import elements_parseError from './elements.parseError';
import elements_wrongContent from './elements.wrongContent';
import elements_v1 from './elements.v1';

const mockups = {
    elements: {
        logicErrors: elements_logicErrors,
        parseError: elements_parseError,
        wrongContent: elements_wrongContent,
        v1: elements_v1,
    }
};

export default mockups;